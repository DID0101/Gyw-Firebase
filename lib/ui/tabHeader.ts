/**
 * In-screen toolbar row under the default tab header (Calls, Stories, Chats).
 * Keeps icon scale consistent across primary tabs — do not override per screen.
 */
export const TAB_HEADER_ICON_SIZE = 20;
