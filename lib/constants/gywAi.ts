export const GYW_AI_SYSTEM_ID = 'gyw_ai_system';
export const GYW_AI_DISPLAY_NAME = 'Gyw AI';

/** User-selected routing for image + caption → vision vs image generation (server also infers when `auto`). */
export type GywAiMultimodalRoutingMode = 'auto' | 'vision' | 'image_gen';

